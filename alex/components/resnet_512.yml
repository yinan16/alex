params_network:
  - type:
      conv
    hyperparams:
      strides:
        - 1
        - 1
      filters:
        shape:
          n_filters:
            512
  - type: batch_normalize
  - type:
      relu

  - type:
      conv
    hyperparams:
      strides:
        - 1
        - 1
      filters:
        shape:
          n_filters:
            512
  - type: batch_normalize
  - type: relu

    name: conv

  - type: add
    input_component:
      - input_component
      - conv
  # - type: batch_normalize
